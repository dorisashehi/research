Step 1: Install Python on Windows
    1. Download Python from: https://www.python.org/downloads/
    2. IMPORTANT: During installation, check the box that says "Add Python to PATH"
    3. Complete the installation

Step 2: Verify in Git Bash or Linux Terminal or iOS Terminal
    After installing, close and reopen Git Bash directly in the folder with files. If using Linux/iOS Termianl reopen and navigate to the folder, then test:

        python --version
        pip --version

Step 3:
	PowerShell/Terminal:
		- go into folder with visualization files
		- run - >  python -m http.server 8000 (webpage with vizualization should open)
		! If nothing happened -> go in browser -> in url box type -> http://localhost:8000/